import '../../../../../exports.dart';

class ArticleType extends StatelessWidget {
  const ArticleType({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
